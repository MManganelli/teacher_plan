<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All coachings
    </h1>

    <a href="/coachings/new" class="btn btn-block btn-outline-secondary">
      Add a new coaching
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "coaching_templates/coachings_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Improvement plan
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @coachings.each do |coaching| %>
      <tr>
        <!-- Display coaching.id start -->
        <td>
          <%= coaching.id %>
        </td>
        <!-- Display coaching.id end -->

        <!-- Display coaching.improvement_plan_id start -->
        <td>
          <% if coaching.implementation_plan.present? %>
            <a href="/improvement_plans/<%= coaching.implementation_plan.id %>">
              <%= coaching.implementation_plan.description %>
            </a>
          <% end %>
        </td>
        <!-- Display coaching.improvement_plan_id end -->

        <!-- Display coaching.user_id start -->
        <td>
          <% if coaching.user.present? %>
            <a href="/users/<%= coaching.user.id %>">
              <%= coaching.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display coaching.user_id end -->

        <!-- Display coaching.created_at start -->
        <td>
          <%= time_ago_in_words(coaching.created_at) %> ago
        </td>
        <!-- Display coaching.created_at end -->

        <!-- Display coaching.updated_at start -->
        <td>
          <%= time_ago_in_words(coaching.updated_at) %> ago
        </td>
        <!-- Display coaching.updated_at end -->

        <td>
          <a href="/coachings/<%= coaching.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @coachings, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
